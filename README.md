# openai-context-summary-demo

Static demo of AI-generated research summaries built from fixed context files and a shared prompt.

- Pages are built with Astro from generated artifacts.
- All run folders found in the private project are exported.
- JSON outputs are included alongside the static site.
- Results can vary across runs because live web search is enabled.
